<!-- <h2>All Bell Points</h2>

<#% @bell.each do |bell| %>
  <#%= link_to image_tag(bell.image.url(:thumb)), admin_artifact_path(bell.id), class: "index_image" %>
  <p>Artifact UI: <%= bell.ui %></p>
  <p>Artifact Point Type: <%= bell.point_type %></p>
<#% end %>

<#%= link_to "Add a new Artifact", new_admin_artifact_path, class: "btn btn-primary" %> -->
